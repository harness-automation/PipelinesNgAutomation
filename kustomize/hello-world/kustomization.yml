# Example configuration for the webserver
# at https://github.com/monopole/hello
commonLabels:
  app: hello-bogdan

data:
  altGreeting: "Good Morning Bogdan!"
  enableRisky: "false"

resources:
- deployment.yaml
- service.yml
- configMap.yml
