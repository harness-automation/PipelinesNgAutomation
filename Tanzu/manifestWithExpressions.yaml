applications:
- name: ((NAME))
  health-check-type: process
  timeout: <+secrets.getValue("account.TAS_Timeout")>
  instances: ((INSTANCE))
  memory: <+secrets.getValue("memory")>
  routes:
    - route: ((ROUTE))
