applications:
- name: ((NAME))
  health-check-type: process
  timeout: <+serviceVariables.timeout>
  instances: <+serviceVariables.Count>
  memory: <+secrets.getValue("memory")>
  routes:
    - route: ((ROUTE))
