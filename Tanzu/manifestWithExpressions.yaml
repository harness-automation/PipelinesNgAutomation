applications:
- name: ((NAME))
  health-check-type: process
  timeout: <+stage.variables.timeout>
  instances: <+stage.variables.Count>
  memory: <+secrets.getValue("memory")>
  routes:
    - route: ((ROUTE))
