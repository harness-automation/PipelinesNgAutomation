# Generated by chatgpt
apiVersion: apiregistration.k8s.io/v1
kind: APIService
metadata:
  name: {{.Values.name}}-crd.sample.com
spec:
  group: sample.com
  version: v1
  groupPriorityMinimum: 1000
  versionPriority: 15
  service:
    name: {{.Values.name}}-crd-service
    namespace: default
  versionMappings:
    - name: v1
      kind: MyCustomResource
      mapping:
        name: {{.Values.name}}-crd-mapping
        kind: {{.Values.name}}-crd
        group: sample.com
        version: v1