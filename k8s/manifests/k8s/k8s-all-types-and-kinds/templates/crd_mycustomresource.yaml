# Example generated by chatgpt
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: {{.Values.name}}-crd.sample.com
spec:
  group: sample.com
  versions:
    - name: v1
      served: true
      storage: true
  scope: Namespaced
  names:
    plural: {{.Values.name}}-crds
    singular: {{.Values.name}}-crd
    kind: {{.Values.name}}-crd
    shortNames:
      - mcr