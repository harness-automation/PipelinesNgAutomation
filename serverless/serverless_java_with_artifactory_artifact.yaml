service: serverless_java_with_artifactory_artifact
frameworkVersion: '2.72.3'

provider:
  name: aws
  runtime: java8

package:
  artifact: <+artifact.image>

functions:
  currentTime:
    handler: com.serverless.Handler
    events:
      - httpApi:
          path: /time
          method: get
