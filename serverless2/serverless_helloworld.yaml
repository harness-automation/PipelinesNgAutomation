service: hello-world-website
frameworkVersion: '3.30.1'

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1
  
functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: hello
          method: get
          cors: true
package:
  artifact: <+artifact.path>
