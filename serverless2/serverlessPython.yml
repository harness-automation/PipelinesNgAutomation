service: newcheckstry
frameworkVersion: '3'

provider:
  name: aws
  runtime: python3.9

package:
  artifact: <+artifact.path>

functions:
  currentTime:
    handler: com.serverless.Handler
    # image: <+artifact.image>
    events:
      - httpApi:
          path: /time
          method: get
